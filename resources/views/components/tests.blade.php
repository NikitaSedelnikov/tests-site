<div class="create-line"></div>
<div class="tests">
    <div align="center" >
    <a class="btn btn-secondary" href="{{ route('test-ready', ['id'=>$test->id]) }}" style="font-size:20px;">{{$test->title}}</a>
    <p class="lead" align="center">{{$test->description}}</p>
    </div>

</div>
